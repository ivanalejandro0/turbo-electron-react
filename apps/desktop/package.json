{
  "name": "desktop",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "# bundle": "# manually copy dependency, so no bundler is needed",
    "bundle": "mkdir -p ./dist/node_modules && cp -R ../../packages/wait-for-server-up/dist/ ./dist/node_modules/wait-for-server-up",
    "watch": "tsc -w",
    "lint": "eslint -c .eslintrc --ext .ts ./src",
    "start": "electron ./dist/main.js"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.27.0",
    "@typescript-eslint/parser": "^5.27.0",
    "electron": "^19.0.3",
    "eslint": "^8.17.0",
    "typescript": "^4.7.3"
  },
  "dependencies": {
    "wait-for-server-up": "*"
  }
}
